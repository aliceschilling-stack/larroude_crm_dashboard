name: Update CRM Dashboard

# Disabled — dashboard is now generated locally by refresh_cowork.py
# and pushed to this repo via run_all_dashboards.sh (launchd nightly job).
# The old update_dashboard.py used a simplified template and was overwriting
# the full dashboard. Re-enable only if moving generation to GitHub Actions.

on:
  workflow_dispatch:   # manual trigger only — no schedule

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - name: Skipped
        run: echo "Automatic generation disabled — dashboard pushed from local machine."
